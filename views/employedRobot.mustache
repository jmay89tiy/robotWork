<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Employed Robots</title>
    <link href="https://fonts.googleapis.com/css?family=Muli" rel="stylesheet">
    <link rel="stylesheet" href="./styles.css">
  </head>
  <body>

    <h1 class="Snagabot"><font color="#CF5300">Employed-Snag-A-Bots</font>
    </h1>
    <div class="links">
    <a class="hireLink" href="hireRobot">Robots for Hire</a>
    <a class= "robotPage" href="/">Home</a>
    </div>


    <div class="users">
      {{#robots}}

      <a href="{{id}}"><div class="single-user">
        <div class="avatar">
          <img src="{{avatar}}">
          <!--  { regular mustaches mean it targets just that piece} -->
        </div>
        <div class="bottomUsers"
        <h4>{{name}}</h4>
        <br></br>

        <p id="location">
          {{#address.city}}
          <!-- any time hash tag is used it means loop -->
            {{address.city}}, {{address.country}}
            <!-- one bar above  -->
          {{/address.city}}
          <!-- everything is closed out here using the '/' -->
          {{^address.city}}
          <!-- ^ this means "not" i.e. similiar to if not then xxx -->
            {{address.country}}
            <!-- one bar above -->
          {{/address.city}}
          {{#job}}
            {{job}}, {{company}}
          {{/job}}
          {{^job}}
            <font color="#CF5300"><br>Available for hire.</br></font>
            <!-- example above means if no job, then he's for hire -->
          {{/job}}
        </p>
      </div>
      </div>
    </a>

    <!-- this starts the bottom section  -->

      {{/robots}}
    </div>       <!-- this ends users here -->
  </body>
</html>
